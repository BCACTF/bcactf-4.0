1. Exploit the eval function to import os. You must input an operator, so use + and an empty string.
2. Further exploit the eval function to list files in the directory with system("ls").
3. Use the exploit again to read the contents of flag.txt.